import { ButtonTypes } from "../types/components";
declare const PrimaryButton: ({ children, link, func, type }: ButtonTypes) => import("react/jsx-runtime").JSX.Element;
export default PrimaryButton;
